<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.test.mapper.TestMapper">

    <select id="selectAll" resultType="java.lang.Object">
        SELECT * FROM module_b_table
    </select>

    <select id="selectById" resultType="java.lang.Object" parameterType="java.lang.Long">
        SELECT * FROM module_b_table WHERE id = #{id}
    </select>

    <insert id="insert">
        INSERT INTO module_b_table (name) VALUES (#{name})
    </insert>

    <update id="updateById">
        UPDATE module_b_table SET name = #{name} WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="java.lang.Long">
        DELETE FROM module_b_table WHERE id = #{id}
    </delete>

</mapper>
